import { addIcons } from 'ionicons';
import { close, copyOutline, folderOpenOutline, homeSharp, informationCircleSharp, optionsSharp, pause, pencil, play, playBack, playForward, playSkipBack, playSkipForward, refresh, searchCircle, searchCircleOutline, shareSocial, trash } from 'ionicons/icons';

// register Ionic icons
export function registerIonicIcons() {
  addIcons({
    close,
    copyOutline,
    folderOpenOutline,
    homeSharp,
    informationCircleSharp,
    optionsSharp,
    pause,
    pencil,
    play,
    playBack,
    playForward,
    playSkipBack,
    playSkipForward,
    refresh,
    searchCircle,
    searchCircleOutline,
    shareSocial,
    trash,
  });
}
