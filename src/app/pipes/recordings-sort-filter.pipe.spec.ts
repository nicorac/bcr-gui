import { RecordingsSortFilterPipe } from './recordings-sort-filter.pipe';

describe('RecordingsSortFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RecordingsSortFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
